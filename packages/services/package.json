{
  "name": "@rebound/services",
  "version": "0.1.0",
  "private": true,
  "description": "Service interfaces + Local implementations. This package existing is the offline guarantee: apps/web + services alone is a complete game (ARCHITECTURE §3).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
