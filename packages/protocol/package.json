{
  "name": "@rebound/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Project Rebound wire protocol — the one shared truth between web client and game server. Schemas, codec, and zod boundary validation (ARCHITECTURE §3).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rebound/sim-core": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
