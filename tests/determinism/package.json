{
  "name": "@rebound/tests-determinism",
  "version": "0.1.0",
  "private": true,
  "description": "Headless determinism suite for sim-core — a red run here is stop-ship (docs/CODING_STANDARDS.md §10).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rebound/sim-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
